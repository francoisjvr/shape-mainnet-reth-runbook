# Legacy Geth Context

This repo does not treat `op-geth` as the primary Shape mainnet path.

Legacy geth context is preserved only for:
- rollback during migration
- historical comparison
- archival notes from the pre-Reth operator workflow

If you need a geth-focused recovery path, use the older archival repos instead of trying to turn this repo back into a geth guide.
